load("//:kernel.bzl", "kernel_configuration")
load("//:kernel.bzl", "kernel_headers")

kernel_configuration(
  name = "kernel",
  srcs = ["@linux//:sources"],
)

kernel_headers(
  name = "headers",
  cfg = ":kernel",
)
